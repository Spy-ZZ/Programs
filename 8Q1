 Design an HTML form to accept two strings from the user. Write a PHP script for the 
following.
a. Find whether the small string appears at the start of the large string. 
b. Find the position of the small string in the big string. 
c. Compare both the string for first n characters, also the comparison should not be case 
sensitive. 

////////////////////////////////////////////////////////////////////////

HTML-:

<html>
<head>
</head>
<body>
	<form action="setb1.php" method="POST">
	Enter first string <input type="text" name="S1"><br><br>
	Enter second string <input type="text" name="S2"><br><br><br>
	Find if small string is at the start of large string <input type="radio" value=1 name="op"><br><br>
	Find the position of small string in large <input type="radio" value=2 name="op"><br><br>
	Comparing both string for first n character <input type="radio" value=3 name="op"><br><br>
	<input type="submit" value="FIND">
	<input type="reset" value="CLEAR">
	</form>
</body>
</html>

PHP-:

<?php
 $str1 = $_POST["S1"];
 $str2 = $_POST["S2"];
 $ch = $_POST["op"];
 switch($ch)
  {
    case 1:find($str1,$str2);
	   break;
    case 2:find_pos($str1,$str2);
	   break;
    case 3:compare($str1,$str2);
	   break;
    default:echo "INVALID";
  }
  function find($str1,$str2)
  {
   if( strlen($str1) > strlen($str2))
   {
    $temp = substr($str1 , 0 , strlen($str2));
     if(strcasecmp($temp,$str1))
       echo "String is not Present";
     else
       echo "String is Present";
     }
   else
    {
       $temp = substr($str2,0,strlen($str1));
       if(strcasecmp($temp,$str1))
	        echo "Not Present";
       else
	        echo "Present";
      }
    }

   

   function find_pos($str1,$str2)
    {
     	if(strlen($str1)>strlen($str2))
          echo strpos($str1,$str2);
      else
          echo strpos($str2,$str1);
    }

   function compare($str1,$str2)
    {
     if(strlen($str1)>strlen($str2))
     {
      if ( strncmp($str1, $str2 , strlen($str2)))
	       echo "Not Equal";
      else
	       echo "Equal";
     }
      else
      {
	      if ( strncmp($str2, $str1 , strlen($str1)))
	         echo "Not Equal";
	      else
	         echo "Equal";
      }
    }
?>
