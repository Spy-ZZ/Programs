Design a form to accept string from the user and perform the following operations
a. To select first 5 words from the string
b. Convert the given string to lowercase and then to Title case.
c. Pad the given string with “*” from left and right both the sides. 
d. Remove the leading whitespaces from the given string. 
e. Find the reverse of given string.
////////////////////////////////////////////////////////

HTML-:

<html>
<body>
<form action ="A2.php" method="POST">
   enetr string<input type="text" name="str"><br><br>
    find length of string <input type="radio" value=1 name="op"><br><br>
    find total number of vowels <input type="radio" value=2 name="op"><br><br>
    convert into lowercase <input type="radio" value=3 name="op"><br><br>
    padding of given string <input type="radio" value=4 name="op"><br><br>
    remove whitespae <input type="radio" value=5 name="op"><br><br>
    reserved string<input type="radio" value=6 name="op"><br><br>
    <input type="submit" value="FIND">
    <input type="rest" value="CLEAR">
 </body>
 </html>


                                                                                        
////////////////////////////////////////////////////////////////////////////////////

PHP-:

<?php

	$s=$_POST["str"];
	$ch=$_POST["op"];

switch($ch)
{
case 1:find_len($s);
        break;
case 2:find_count($s);
        break;
case 3:conv_low($s);
        break;
case 4:padding($s);
                break;
case 5:remove_space($s);
        break;
case 6:rev($s);
        break;
default:echo "INVALID";
 }
function find_len($a)
{
 echo $a;
  $l=0;
  $i=0;
  while($a[$i]!=NULL)
  {
  $l++;
  $i++;
  }
   echo "Lenght of string is $l";
}
function find_count($a)
{
 $a1=0;
 $e=0;
 $i1=0;
 $o=0;
 $u=0;
  for($i=0;$i<strlen($a);$i++)  
  {
   if($a[$i]=="a")
     $a1++;
   else if($a[$i]=="e")
     $e++;
   else if($a[$i]=="i")
     $i1++;
   else if($a[$i]=="o")
     $o++;
  else if($a[$i]=="u")
     $u++;
   else
    continue;
  }
  echo "A = $a1";
  echo "E = $e";
  echo "I = $i1";
  echo "O = $o";
 echo "U = $u";
}
function conv_low($a)
{
 $str=strtolower($a);
 print_r($str);
}
function padding($a)
{
 echo str_pad($a,25,"world",STR_PAD_RIGHT);
}
function remove_space($a)
{ 
 echo trim($a);
}
function rev($a)
{
 $result=strrev($a);
 echo "$result";
}

?>

