# # Slip 10 

# Q.2
# A) Write a python program to Display column-wise mean, and median for SOCR-HeightWeight dataset. [10]

import pandas as pd
df = pd.read_csv("Downloads/weight-height.csv")
print(df.mean())
print("\n")
print(df.median())



# B) Write a python program to compute sum of Manhattan distance between all pairs of 
# points. [5]

def get_manhattan_distance(p, q):
    distance = 0
    for p_i,q_i in zip(p,q):
        distance += abs(p_i - q_i)
    return distance
a = (1, 1)
b = (4, 3)
d = get_manhattan_distance(a, b)
print(d)

